/**
 * Single source of truth for the developer's current location string.
 * Displayed in the CV, about section, and hero components.
 * Update this value when the location changes rather than editing each consumer directly.
 * @author Maruf Bepary
 */
const location: string = "London, United Kingdom";

export default location;
