/**
 * This is the name that will be used as the developer name in the application.
 * Wherever the developer name is needed, this name will be used.
 * This would allow the developer to change the name in one place and it will be reflected everywhere.
 */
const developerName: string = "Maruf Bepary";

export default developerName;
